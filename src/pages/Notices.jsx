import React, { useEffect, useState } from 'react';
import { Bell, Pencil, Plus, Trash2, X } from 'lucide-react';
import { Modal } from '../components/common/Modal';
import { getNotices, saveNotices } from '../services/notices.storage';

const EMPTY_NOTICE = { title: '', message: '', priority: 'normal', target: 'All Departments', date: '' };

const priorityClass = { high: 'text-rose-300 bg-rose-950/30 border-rose-500/30', normal: 'text-sky-300 bg-sky-950/30 border-sky-500/30', low: 'text-slate-300 bg-slate-800 border-slate-700' };

export const Notices = () => {
  const [notices, setNotices] = useState(() => getNotices());
  const [form, setForm] = useState(EMPTY_NOTICE);
  const [editingId, setEditingId] = useState(null);
  const [open, setOpen] = useState(false);
  useEffect(() => { saveNotices(notices); }, [notices]);
  const close = () => { setOpen(false); setEditingId(null); setForm(EMPTY_NOTICE); };
  const submit = (event) => { event.preventDefault(); if (!form.title.trim() || !form.message.trim()) return; setNotices((current) => { const updated = editingId ? current.map((item) => item.id === editingId ? { ...item, ...form } : item) : [...current, { ...form, id: `notice-local-${Date.now()}`, date: form.date || new Date().toISOString().slice(0, 10) }]; saveNotices(updated); return updated; }); close(); };
  const deleteNotice = (noticeId) => { setNotices((current) => { const updated = current.filter((item) => item.id !== noticeId); saveNotices(updated); return updated; }); };
  return <div className="flex-1 overflow-y-auto p-6 max-w-6xl mx-auto w-full space-y-6 animate-in fade-in duration-200"><div className="flex flex-col sm:flex-row sm:items-center justify-between gap-4 border-b border-slate-800 pb-4"><div><h1 className="text-lg font-bold text-slate-100 flex items-center gap-2.5"><Bell className="text-amber-400" size={20} /><span>Notices</span></h1><p className="text-xs text-slate-400 mt-1">Publish local operational notices for workbench departments.</p></div><div className="flex items-center gap-2"><span className="text-[10px] font-mono text-amber-300 bg-amber-950/30 border border-amber-500/30 rounded-lg px-3 py-1.5">LOCAL MOCK DATA</span><button type="button" onClick={() => { setForm(EMPTY_NOTICE); setEditingId(null); setOpen(true); }} className="inline-flex items-center gap-1.5 px-3.5 py-2 rounded-lg bg-amber-500 hover:bg-amber-400 text-slate-950 text-xs font-semibold"><Plus size={14} />Create Notice</button></div></div><div className="space-y-3">{notices.map((notice) => <article key={notice.id} className="bg-[#0e121a] border border-slate-800 rounded-xl p-5 shadow-lg"><div className="flex flex-col md:flex-row md:items-start justify-between gap-4"><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><h2 className="text-sm font-semibold text-slate-100">{notice.title}</h2><span className={`px-2 py-0.5 rounded border text-[10px] font-mono uppercase ${priorityClass[notice.priority]}`}>{notice.priority}</span></div><p className="text-xs text-slate-400 mt-2 leading-relaxed">{notice.message}</p><div className="flex flex-wrap gap-4 mt-3 text-[10px] font-mono text-slate-500"><span>Target: <strong className="text-slate-300">{notice.target}</strong></span><span>Date: <strong className="text-slate-300">{notice.date}</strong></span></div></div><div className="flex items-center gap-1.5"><button type="button" onClick={() => { setForm(notice); setEditingId(notice.id); setOpen(true); }} className="inline-flex items-center gap-1 px-2.5 py-1.5 rounded-md bg-slate-800 text-slate-300 text-[11px]"><Pencil size={12} />Edit</button><button type="button" onClick={() => setNotices((current) => current.filter((item) => item.id !== notice.id))} className="p-1.5 text-slate-500 hover:text-rose-300"><Trash2 size={13} /></button></div></div></article>)}</div><Modal isOpen={open} onClose={close} title={editingId ? 'Edit Notice' : 'Create Notice'}><form onSubmit={submit} className="space-y-4 text-xs font-mono"><label className="block text-slate-400">Title<input required value={form.title} onChange={(event) => setForm({ ...form, title: event.target.value })} className="mt-1 w-full bg-[#080a0f] border border-slate-700 rounded-lg px-3 py-2 text-slate-200" /></label><label className="block text-slate-400">Message<textarea required rows={4} value={form.message} onChange={(event) => setForm({ ...form, message: event.target.value })} className="mt-1 w-full bg-[#080a0f] border border-slate-700 rounded-lg px-3 py-2 text-slate-200" /></label><div className="grid grid-cols-1 sm:grid-cols-3 gap-3"><label className="text-slate-400">Priority<select value={form.priority} onChange={(event) => setForm({ ...form, priority: event.target.value })} className="mt-1 w-full bg-[#080a0f] border border-slate-700 rounded-lg px-3 py-2 text-slate-200"><option value="high">High</option><option value="normal">Normal</option><option value="low">Low</option></select></label><label className="text-slate-400">Target<input value={form.target} onChange={(event) => setForm({ ...form, target: event.target.value })} className="mt-1 w-full bg-[#080a0f] border border-slate-700 rounded-lg px-3 py-2 text-slate-200" /></label><label className="text-slate-400">Date<input type="date" value={form.date} onChange={(event) => setForm({ ...form, date: event.target.value })} className="mt-1 w-full bg-[#080a0f] border border-slate-700 rounded-lg px-3 py-2 text-slate-200" /></label></div><div className="flex justify-end gap-2 border-t border-slate-800 pt-3"><button type="button" onClick={close} className="inline-flex items-center gap-1.5 px-3 py-1.5 rounded-lg bg-slate-800 text-slate-300"><X size={13} />Cancel</button><button type="submit" className="px-4 py-1.5 rounded-lg bg-amber-500 text-slate-950 font-semibold">{editingId ? 'Save Changes' : 'Publish Notice'}</button></div></form></Modal></div>;
};

export default Notices;
